# 控制台截图占位 · Console Screenshot Placeholders

把下面三类截图放进本目录（`docs/assets/`），指南里的占位引用即会自动渲染。
Drop the three screenshots below into this folder (`docs/assets/`); the placeholders in the guides will then render automatically.

| 文件名 (File) | 中文说明 | English |
| :--- | :--- | :--- |
| `console-overview.png` | 控制台首屏：版本徽标、状态卡（服务在线/本机 IP/连接数）、`local-plugin 已连接`、暗色主题。 | Console home: version badge, Status card (server online / local IP / connection count), `local-plugin connected`, dark theme. |
| `console-tokens.png` | 令牌卡片：「新建令牌」弹窗（label、readonly、自动生成 value）与已保存令牌列表。 | Tokens card: "New token" dialog (label, readonly, auto value) and saved token list. |
| `console-transfer.png` | 传送卡片：选中 webdav/sftp/direct 模式，展开凭据输入框，显示当前生效模式。 | Transfer card: webdav/sftp/direct mode selected, credential fields expanded, active mode shown. |

> 提示：在浏览器打开 http://127.0.0.1:3056 后直接对页面截图即可。建议保持暗色主题、窗口宽度 ≥ 1200px。
> Tip: open http://127.0.0.1:3056 in a browser and screenshot the page. Keep the dark theme and a window width ≥ 1200px.
